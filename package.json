{
  "name": "obsidian-sync-system",
  "version": "1.0.0",
  "description": "Obsidian 3-Way Sync System",
  "private": true,
  "scripts": {
    "postinstall": "pnpm install --filter obsidian-sync-bridge",
    "build:plugin": "pnpm --filter obsidian-sync-bridge build"
  },
  "dependencies": {
    "obsidian-headless": "latest"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "better-sqlite3"
    ]
  }
}
